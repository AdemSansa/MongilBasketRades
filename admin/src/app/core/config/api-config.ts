// Dev machine's LAN IP so this can be tested from a browser on another
// machine on the network, same as the Flutter app's ApiConstants. Swap
// for a real deployed URL once the backend is hosted somewhere.
export const API_BASE_URL = 'http://192.168.100.105:8080/api';

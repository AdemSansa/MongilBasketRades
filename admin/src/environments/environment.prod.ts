// Production build (ng build --configuration production), swapped in via
// angular.json's fileReplacements. Update this once the Render backend
// service URL is known (see docs/deployment/DEPLOY.md).
export const environment = {
  apiBaseUrl: 'https://REPLACE-WITH-RENDER-BACKEND-URL.onrender.com/api',
};

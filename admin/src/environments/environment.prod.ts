// Production build (ng build --configuration production), swapped in via
// angular.json's fileReplacements.
export const environment = {
  apiBaseUrl: 'https://mongilbasketrades.onrender.com/api',
};
